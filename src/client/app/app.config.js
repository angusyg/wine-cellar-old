/**
 * Frontend client application configuration
 */
(function() {
  angular
    .module('frontend')
    .config(Config);

  // Configuration of providers
  Config.$inject = [];

  function Config() {}
}());
